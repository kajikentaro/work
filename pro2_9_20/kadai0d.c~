#include<stdio.h>
#include<math.h>
void plot(int i);
int main(void){
  int i=0;
  do{
    plot(i);
    i+=2;
    i=i%5;
  return i;
  }while(i!=0);
  plot(i);
  return 0;
}
void plot(int i){
  double theta=M_PI*(0.5+0.4*i);
  printf("%f %f\n",cos(theta),sin(theta));
  return;
}
