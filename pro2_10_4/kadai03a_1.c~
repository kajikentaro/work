#include<stdio.h>
int main(void){
    int num;
    printf("10桁以下の非負整数を入力してください:");
    scanf("%d",&num);
    int i;
    int sum=0;
    for(i=0;i<10;i++){
        sum+=num%10;
        num/=10;
    }
    printf("%d\n",sum);
}
